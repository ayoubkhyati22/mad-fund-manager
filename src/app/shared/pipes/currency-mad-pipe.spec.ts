import { CurrencyMadPipe } from './currency-mad-pipe';

describe('CurrencyMadPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyMadPipe();
    expect(pipe).toBeTruthy();
  });
});
